pre-commit:
  commands:
    rubocop:
      tags: backend style
      glob: "*.rb"
      exclude: '(^db/)|(^bin/)|(^vendor/)|(^node_modules/)'
      run: bin/rubocop --force-exclusion {staged_files}
    brakeman:
      tags: security
      run: bin/brakeman -q --no-pager --no-summary
    audit:
      tags: security
      run: bin/bundler-audit check --update
    importmap-audit:
      tags: security
      run: bin/importmap audit
